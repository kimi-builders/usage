name: Release npm package

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      release_tag:
        description: Existing v-prefixed release tag to recover
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: npm-release-${{ github.event.release.tag_name || inputs.release_tag }}
  cancel-in-progress: false

jobs:
  collector:
    name: Release gate · ${{ matrix.label }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: ubuntu-latest, node: 20, label: Linux · Node 20 compatibility }
          - { os: ubuntu-latest, node: 22, label: Linux · Node 22 LTS }
          - { os: ubuntu-latest, node: 24, label: Linux · Node 24 LTS }
          - { os: macos-latest, node: 22, label: macOS · Node 22 LTS }
          - { os: macos-latest, node: 24, label: macOS · Node 24 LTS }
          - { os: windows-latest, node: 22, label: Windows · Node 22 LTS }
          - { os: windows-latest, node: 24, label: Windows · Node 24 LTS }
    steps:
      - name: Check out release source
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ github.event.release.tag_name || inputs.release_tag }}
      - name: Set up Node.js
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f # v6.1.0
        with:
          node-version: ${{ matrix.node }}
          package-manager-cache: false
      - name: Install Collector package
        run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Test Collector and provider contracts
        run: npm run test:collector

  publish:
    name: Build, attest, and publish
    needs: collector
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Check out release source
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ github.event.release.tag_name || inputs.release_tag }}
      - name: Set up Node.js and npm registry
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f # v6.1.0
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
          package-manager-cache: false
      - name: Use an OIDC-capable npm CLI
        run: npm install --global npm@12.0.1 --ignore-scripts --no-audit --no-fund
      - name: Verify release tag matches package version
        env:
          RELEASE_TAG: ${{ github.event.release.tag_name || inputs.release_tag }}
        run: node -e "const version=require('./package.json').version; if (process.env.RELEASE_TAG !== 'v'+version) throw new Error('release tag must be v'+version)"
      - name: Install Collector package
        run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Install Dashboard dependencies
        run: npm ci --prefix dashboard --ignore-scripts --no-audit --no-fund
      - name: Run the complete release gate
        run: npm run release:check
      - name: Generate CycloneDX SBOMs
        shell: bash
        run: |
          mkdir -p release
          npm sbom --sbom-format cyclonedx > release/collector.cdx.json
          npm --prefix dashboard sbom --sbom-format cyclonedx > release/dashboard.cdx.json
      - name: Pack the reviewed release artifact
        id: pack
        shell: bash
        run: echo "tarball=release/$(npm pack --ignore-scripts --silent --pack-destination release)" >> "$GITHUB_OUTPUT"
      - name: Upload release evidence
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: npm-release-${{ github.event.release.tag_name || inputs.release_tag }}
          path: release/
          if-no-files-found: error
          retention-days: 90
      - name: Publish reviewed npm artifact
        # npm 12 treats an unprefixed `release/*.tgz` as a GitHub shorthand.
        # The explicit relative prefix guarantees that the reviewed local
        # tarball—and not a remotely resolved package—is what gets published.
        run: npm publish "./${{ steps.pack.outputs.tarball }}" --access public --provenance
        env:
          # Required only for the first publication, before npm has a package
          # settings page on which to configure the trusted publisher. Remove
          # this repository secret immediately after the bootstrap release;
          # npm then authenticates this same step exclusively through OIDC.
          NODE_AUTH_TOKEN: ${{ secrets.NPM_FIRST_PUBLISH_TOKEN }}
